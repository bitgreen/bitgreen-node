import type { Codec, CodecClass, Registry } from '../types';
import { Base } from '../base/Base';
export declare class WrapperOpaque<T extends Codec> extends Base<T> {
    constructor(registry: Registry, Type: CodecClass<T> | string, value?: unknown);
    static with<T extends Codec>(Type: CodecClass<T> | string): CodecClass<WrapperOpaque<T>>;
    /**
     * @description Returns the base runtime type name for this instance
     */
    toRawType(): string;
    /**
     * @description The length of the value when encoded as a Uint8Array
     */
    toU8a(isBare?: boolean): Uint8Array;
}
