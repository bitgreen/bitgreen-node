[profile.release]
panic = 'unwind'

[profile.dev]
split-debuginfo = "unpacked"

[workspace]
members = [
    'parachain',
    'runtime/*',
    'node',
    'pallets/*',
    'primitives'
]
exclude = ['rpc']
